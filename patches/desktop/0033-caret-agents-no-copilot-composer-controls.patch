diff --git a/src/vs/workbench/contrib/chat/browser/actions/chatExecuteActions.ts b/src/vs/workbench/contrib/chat/browser/actions/chatExecuteActions.ts
index ac2ae52c1d7..571958f21ed 100644
--- a/src/vs/workbench/contrib/chat/browser/actions/chatExecuteActions.ts
+++ b/src/vs/workbench/contrib/chat/browser/actions/chatExecuteActions.ts
@@ -446,6 +446,10 @@ export class OpenPermissionPickerAction extends Action2 {
 							ChatContextKeys.lockedToCodingAgent.negate(),
 							ChatContextKeys.lockedCodingAgentId.isEqualTo(AgentSessionProviders.Background),
 						),
+						// Caret: these levels (manual / allow all / autopilot) are the Copilot
+						// permission model. The Agents window's approvals come from OMP, so the
+						// picker would describe a policy nothing enforces.
+						IsSessionsWindowContext.toNegated(),
 					)
 			}
 		});
diff --git a/src/vs/workbench/contrib/chat/browser/actions/chatToolActions.ts b/src/vs/workbench/contrib/chat/browser/actions/chatToolActions.ts
index dbb267b6dc9..2faecdec034 100644
--- a/src/vs/workbench/contrib/chat/browser/actions/chatToolActions.ts
+++ b/src/vs/workbench/contrib/chat/browser/actions/chatToolActions.ts
@@ -17,6 +17,7 @@ import { ContextKeyExpr } from '../../../../../platform/contextkey/common/contex
 import { IInstantiationService } from '../../../../../platform/instantiation/common/instantiation.js';
 import { KeybindingWeight } from '../../../../../platform/keybinding/common/keybindingsRegistry.js';
 import { ITelemetryService } from '../../../../../platform/telemetry/common/telemetry.js';
+import { IsSessionsWindowContext } from '../../../../common/contextkeys.js';
 import { ChatContextKeys } from '../../common/actions/chatContextKeys.js';
 import { ConfirmedReason, IChatToolInvocation, ToolConfirmKind } from '../../common/chatService/chatService.js';
 import { isResponseVM } from '../../common/model/chatViewModel.js';
@@ -130,6 +131,10 @@ export class ConfigureToolsAction extends Action2 {
 				when: ContextKeyExpr.and(
 					ChatContextKeys.chatModeKind.isEqualTo(ChatModeKind.Agent),
 					ChatContextKeys.lockedToCodingAgent.negate(),
+					// Caret: this configures the base's tool set for the default agent, and the
+					// Agents window runs on OMP, which owns its own tools end to end. Offering it
+					// there would promise a setting nothing reads.
+					IsSessionsWindowContext.toNegated(),
 				),
 				id: MenuId.ChatInput,
 				group: 'navigation',
diff --git a/src/vs/workbench/contrib/chat/browser/promptSyntax/chatModeActions.ts b/src/vs/workbench/contrib/chat/browser/promptSyntax/chatModeActions.ts
index b20fd41934a..1c538a85d60 100644
--- a/src/vs/workbench/contrib/chat/browser/promptSyntax/chatModeActions.ts
+++ b/src/vs/workbench/contrib/chat/browser/promptSyntax/chatModeActions.ts
@@ -5,6 +5,7 @@
 
 import { CHAT_CATEGORY } from '../actions/chatActions.js';
 import { Codicon } from '../../../../../base/common/codicons.js';
+import { IsSessionsWindowContext } from '../../../../common/contextkeys.js';
 import { ChatContextKeys } from '../../common/actions/chatContextKeys.js';
 import { localize, localize2 } from '../../../../../nls.js';
 import { ServicesAccessor } from '../../../../../editor/browser/editorExtensions.js';
@@ -31,10 +32,13 @@ function createConfigureAgentsActionConfig(disabled: boolean) {
 		title: localize2('configure-agents', "Configure Custom Agents..."),
 		category: CHAT_CATEGORY,
 		f1: !disabled,
-		precondition: disabled ? ContextKeyExpr.false() : ContextKeyExpr.and(ChatContextKeys.enabled, agentModeDisabledByPolicy.negate()),
+		// Caret: custom agents are a Copilot-chat concept and Caret has one harness (OMP), so
+		// the Agents window shows no entry that would open a surface which does not exist
+		// there. The IDE window keeps it.
+		precondition: disabled ? ContextKeyExpr.false() : ContextKeyExpr.and(ChatContextKeys.enabled, agentModeDisabledByPolicy.negate(), IsSessionsWindowContext.toNegated()),
 		menu: {
 			id: MenuId.ChatModePicker,
-			when: ContextKeyExpr.and(ChatContextKeys.enabled, disabled ? agentModeDisabledByPolicy : agentModeDisabledByPolicy.negate()),
+			when: ContextKeyExpr.and(ChatContextKeys.enabled, disabled ? agentModeDisabledByPolicy : agentModeDisabledByPolicy.negate(), IsSessionsWindowContext.toNegated()),
 		},
 	};
 	return disabled ? { ...base, icon: Codicon.lock, tooltip: localize('managedByOrganization', "Managed by your organization") } : base;
