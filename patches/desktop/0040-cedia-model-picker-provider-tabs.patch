diff --git a/src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts b/src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts
index cec9b29bf6c..d2b8f71bad5 100644
--- a/src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts
+++ b/src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts
@@ -295,6 +295,12 @@ configurationRegistry.registerConfiguration({
 			default: false,
 			tags: ['experimental'],
 			experiment: { mode: 'auto' },
+			// Cedia: the Agents window runs one provider per model (OMP's own
+			// catalogue names the provider that executes the turn), so the flat
+			// promoted list hid which provider a row belonged to. The tabbed picker
+			// is the reference shape and is on by default here only; the IDE window
+			// keeps the flat picker.
+			agentsWindow: { default: true },
 		},
 		'chat.fontSize': {
 			type: 'number',
