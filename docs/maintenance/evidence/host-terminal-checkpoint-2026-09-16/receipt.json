{
  "slice": "host-terminal-checkpoint",
  "date": "2026-09-16",
  "planItem": "23 (first slice: the state half of the Ghostty VT work)",
  "evaluation": "../terminal-vt-spike-2026-09-16/",
  "gap": "the mobile renderer replays a bounded chunk history (4 MiB / 4096 chunks); after a trim or a reconnect there is nothing to restore the screen from, so the client can only ask OMP for a redraw - its own receipt says 'fresh redraw recovery is not a serialized terminal checkpoint' (mobile-terminal-recovery-2026-09-13)",
  "change": {
    "apps/host/src/terminal-state.ts": "TerminalStateRegistry: one headless screen per terminal fed from caret_terminal_open/output/close with the clients' sequence rules (duplicates ignored, a gap marks historyIncomplete, reopen keeps the screen and follows the new size, close keeps the final screen); the engine loads lazily and is optional - without the native package the registry keeps nothing and the session runs as before",
    "apps/host/src/service.ts": "registry created per runtime when the virtual UI is on, fed from #onFrame (the same hook that records events), resized when a caret_terminal_resize command is acknowledged, disposed with the runtime; terminalSnapshots(sessionId) exposes the checkpoints",
    "apps/host/src/router.ts": "GET /v1/sessions/:id/terminals -> { terminals: [...] }; 404 for an unknown session",
    "packages/protocol/src/index.ts": "TerminalCheckpoint (terminalId, title, cols/rows, cursor, lines, lastSequence, closed/closeReason, historyIncomplete)",
    "apps/host/package.json": "@coder/libghostty-vt-node pinned at 0.1.0-beta.0 (libghostty-vt, MIT)",
    "scripts/build-caret.ts": "the engine is a native addon: external to the host bundle, and the package (prebuilds + node-gyp-build) is copied beside it; the build throws when the platform has no prebuild",
    "docs/upstream-notices/": "ghostty-LICENSE.txt and libghostty-vt-node-LICENSE.txt (both MIT)"
  },
  "verified": {
    "unit": [
      "bun test apps/host/test/terminal-state.test.ts: 6 pass - one screen per terminal from open/output/close, duplicate sequence ignored, real gap sets historyIncomplete, reopen keeps the screen, resize after acknowledgement (closed terminals stay closed), no-engine path keeps running and reports no checkpoints",
      "the real engine: the prebuild loads on darwin-arm64 and renders the fed stream (plain row + coloured row)"
    ],
    "route": "bun test apps/host/test/router.test.ts: the route returns { terminals: [] } for a session with no frames, 404 for an unknown session, and passes a checkpoint through unchanged",
    "build": "bun scripts/build-caret.ts ships @coder/libghostty-vt-node + node-gyp-build under dist/host/node_modules; the shipped copy loads under plain Node and renders a row",
    "endToEnd": "bun scripts/omp-virtual-ui-smoke.ts against real OMP with a scripted loopback model (no provider credentials): 15 checks pass, including 'host-terminal-checkpoint-matches-omp-frames' - the checkpoint matches the negotiated open frame's cols/rows, shows the startup output and records a real output sequence; full receipt in virtual-ui-smoke.json",
    "gates": [
      "bun run typecheck: clean",
      "bun run test: 776 tests, 1 failure = menus-contract.test.ts needs rg on PATH (environmental, pre-existing)",
      "node scripts/ci-validate.mjs: CI-OK"
    ]
  },
  "notVerified": [
    "no client consumes the checkpoint yet: the mobile app still reduces frames and asks OMP for a redraw; wiring it into the mobile renderer and the relay path is the rest of item 23",
    "the closure still needs a receipt from a real device path (iPhone over relay or local)",
    "the host keeps state only while a runtime is live; a host restart loses the screen (the frames remain in the session journal, so a later slice can rebuild from those)",
    "caret_terminal_input is not mirrored into the headless screen - OMP echoes input as output frames, which is what the screen follows"
  ]
}
